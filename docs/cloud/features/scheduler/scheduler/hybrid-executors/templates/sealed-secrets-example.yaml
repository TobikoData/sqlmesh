{{- if .Values.secrets.sealedSecrets.enabled }}
# This is an example of a SealedSecret that can be used with this chart
# Replace this with your actual SealedSecret generated using kubeseal
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ include "hybrid-executors.fullname" . }}-sealed-secrets
  labels:
    {{- include "hybrid-executors.labels" . | nindent 4 }}
# The spec below is an example and should be replaced with your actual sealed data
# spec:
#   encryptedData:
#     TCLOUD_CLIENT_SECRET: <sealed-data>
#     # Example of gateway connection secrets (any parameter with 'password', 'secret', or 'token' in the name)
#     SQLMESH__GATEWAYS__GATEWAY_A__CONNECTION__PASSWORD: <sealed-data>
#     SQLMESH__GATEWAYS__GATEWAY_A__CONNECTION__API_TOKEN: <sealed-data>
#     SQLMESH__GATEWAYS__GATEWAY_A__CONNECTION__CLIENT_SECRET: <sealed-data>
#   template:
#     metadata:
#       name: {{ include "hybrid-executors.fullname" . }}-sealed-secrets
#       labels:
#         {{- include "hybrid-executors.labels" . | nindent 8 }}
{{- end }} 